bind 0.0.0.0
port 6379
# option disables protected mode and allows the Redis server to accept connections from clients running on any host
# Set memory limits: To prevent Redis from consuming too much memory,
maxmemory 128mb
maxmemory-policy allkeys-lru